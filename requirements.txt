# Runtime dependencies for the FastAPI backend (Render deploy).
# The API serves the relational DB (PostgreSQL via DATABASE_URL —
# Supabase/Render) + pandas computations. The ML pipeline
# (Prophet) lives in requirements-ml.txt (local training only).
pandas
numpy
openpyxl
fastapi
uvicorn
groq>=0.9.0
# anthropic  # keep commented until the Groq swap is verified end-to-end
sqlalchemy
psycopg2-binary
resend