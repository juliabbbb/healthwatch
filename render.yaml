# Render blueprint for HEALTHWATCH.
# Deploy: Render Dashboard -> New -> Blueprint -> select this repo.
# Secrets (sync: false) are prompted at deploy time and never stored in the file.

services:
  - type: web
    name: healthwatch-api
    runtime: python
    rootDir: .
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ALLOWED_ORIGINS
        value: https://healthwatch-frontend.onrender.com
      - key: GEMINI_API_KEY
        sync: false

  - type: web
    name: healthwatch-frontend
    runtime: node
    rootDir: frontend
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: node .output/server/index.mjs
    envVars:
      - key: VITE_API_URL
        value: https://healthwatch-api.onrender.com
      - key: VITE_CARTO_API_KEY
        sync: false